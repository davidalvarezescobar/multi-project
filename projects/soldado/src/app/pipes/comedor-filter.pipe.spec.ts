import { ComedorFilterPipe } from './comedor-filter.pipe';

describe('ComedorFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ComedorFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
